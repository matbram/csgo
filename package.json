{
  "name": "csgo-clone",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "Browser-based CS:GO clone (Dust 2, bot-only)",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -b --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@babylonjs/core": "^7.40.0",
    "@babylonjs/materials": "^7.40.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
